import { SetTypeDisplayPipe } from './set-type-display.pipe';

describe('SetTypDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SetTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
