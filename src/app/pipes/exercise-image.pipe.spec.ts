import { ExerciseImagePipe } from './exercise-image.pipe';

describe('ExerciseImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseImagePipe();
    expect(pipe).toBeTruthy();
  });
});
